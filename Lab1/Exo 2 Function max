#include <stdio.h>
int max(int x ,int y){
return x;
if (x<y) 
return y;
}


int main(){
    int x,y;
    printf("enter x \n");
    scanf("%d",&x);
     printf("enter y \n");
    scanf("%d",&y);
    printf("  the larger of %d and %d is %d",x,y,max(x,y));
    return 0;
}
